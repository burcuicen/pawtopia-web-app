import React from 'react';
import './styles.scss';

const PFooter: React.FC = () => {
    return (
        <footer className="p-footer">
            Footer
        </footer>
    );
};

export default PFooter;