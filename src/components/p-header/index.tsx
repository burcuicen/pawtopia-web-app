import React from 'react';
import './styles.scss';
const PHeader: React.FC = () => {
    return (
        <header className="header">
            Header
        </header>
    );
};

export default PHeader;